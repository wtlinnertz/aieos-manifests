apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Flux reconciles this directory (per each service's flux/kustomization-staging.yaml,
# path: ./envs/staging). A service's publish.manifest adapter renders its overlay into
# <service>.yaml here and appends it below.
resources:
  - aieos-artifact-store.yaml
